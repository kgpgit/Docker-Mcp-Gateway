# Exemplo de arquivo de variáveis de ambiente para Docker MCP Gateway
# Copie este arquivo para .env e preencha com seus valores

# =============================================================================
# VARIÁVEIS OBRIGATÓRIAS
# =============================================================================

# Socket do Docker (geralmente já configurado no sistema)
# DOCKER_HOST=unix:///var/run/docker.sock

# =============================================================================
# VARIÁVEIS OPCIONAIS - CONFIGURAÇÃO GERAL
# =============================================================================

# Nível de log para o MCP Gateway (debug, info, warn, error)
DOCKER_MCP_LOG_LEVEL=info

# Caminhos personalizados para arquivos de configuração
# DOCKER_MCP_CONFIG_PATH=~/.docker/mcp/config.yaml
# DOCKER_MCP_CATALOG_PATH=~/.docker/mcp/catalogs/docker-mcp.yaml
# DOCKER_MCP_REGISTRY_PATH=~/.docker/mcp/registry.yaml

# =============================================================================
# SECRETS - CREDENCIAIS PARA SERVIÇOS EXTERNOS
# =============================================================================

# GitHub Personal Access Token
# Como obter: GitHub Settings > Developer settings > Personal access tokens
# GITHUB_PERSONAL_ACCESS_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# PostgreSQL Connection String
# Formato: postgresql://username:password@host:port/database
# POSTGRES_CONNECTION_STRING=postgresql://postgres:senha@localhost:5432/meubanco

# Neo4j Database Credentials
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=sua_senha_aqui

# =============================================================================
# SECRETS - OUTROS SERVIÇOS (EXEMPLOS)
# =============================================================================

# API Key para serviços externos
# API_KEY_EXEMPLO=sua_api_key_aqui

# Token de autenticação para serviços remotos
# REMOTE_AUTH_TOKEN=seu_token_aqui

# =============================================================================
# CONFIGURAÇÕES DE REDE (PARA TRANSPORTES HTTP/SSE)
# =============================================================================

# Porta para transporte streaming (padrão: 8080)
# STREAMING_PORT=8080

# Porta para transporte SSE (padrão: 8081)
# SSE_PORT=8081

# Host para conexões HTTP (padrão: localhost)
# HTTP_HOST=localhost

# =============================================================================
# CONFIGURAÇÕES DE RECURSOS
# =============================================================================

# Número de CPUs para cada servidor MCP (padrão: 1)
# MCP_SERVER_CPUS=1

# Memória para cada servidor MCP (padrão: 2Gb)
# MCP_SERVER_MEMORY=2Gb

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA
# =============================================================================

# Bloquear acesso à rede (padrão: false)
# MCP_BLOCK_NETWORK=false

# Bloquear secrets (padrão: true)
# MCP_BLOCK_SECRETS=true

# Verificar assinaturas de imagens (padrão: false)
# MCP_VERIFY_SIGNATURES=false

# =============================================================================
# CONFIGURAÇÕES DE DESENVOLVIMENTO
# =============================================================================

# Ativar modo verbose (padrão: false)
# MCP_VERBOSE=false

# Log de chamadas de ferramentas (padrão: true)
# MCP_LOG_CALLS=true

# Modo de teste (não inicia conexões) (padrão: false)
# MCP_DRY_RUN=false

# =============================================================================
# EXEMPLOS DE CONFIGURAÇÃO PARA SERVIÇOS ESPECÍFICOS
# =============================================================================

# Configuração para servidor de email
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=seu_email@gmail.com
# SMTP_PASSWORD=sua_senha_de_app

# Configuração para serviço de armazenamento
# AWS_ACCESS_KEY_ID=sua_access_key
# AWS_SECRET_ACCESS_KEY=sua_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=seu_bucket

# Configuração para API de notificações
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR_WORKSPACE/YOUR_CHANNEL/YOUR_WEBHOOK_TOKEN
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# =============================================================================
# INSTRUÇÕES DE USO
# =============================================================================

# 1. Copie este arquivo para .env:
#    cp .env.example .env
#
# 2. Edite o arquivo .env com suas credenciais reais
#
# 3. Carregue as variáveis de ambiente:
#    source .env
#    # ou
#    export $(cat .env | xargs)
#
# 4. Use o MCP Gateway com as variáveis carregadas:
#    docker mcp gateway run --servers=github,postgres --secrets=docker-desktop:./.env
#
# 5. Para Docker Compose, as variáveis serão carregadas automaticamente:
#    docker-compose up
#
# =============================================================================
# DICAS DE SEGURANÇA
# =============================================================================

# - NUNCA commit o arquivo .env em repositórios públicos
# - Adicione .env ao seu .gitignore
# - Use secrets management em produção (Docker Secrets, Kubernetes Secrets, etc.)
# - Revogue tokens e chaves que não estão mais em uso
# - Use tokens com o mínimo de permissões necessárias
#
# =============================================================================
# REFERÊNCIAS
# =============================================================================

# Documentação oficial: https://docs.docker.com/mcp-gateway/
# Exemplos de configuração: ./examples/
# Guia de troubleshooting: ./docs/troubleshooting.md