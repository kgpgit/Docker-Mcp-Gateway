# =============================================================================
# Docker MCP Gateway - Variáveis de Ambiente Otimizadas
# =============================================================================
# Este arquivo contém todas as variáveis de ambiente necessárias para a
# configuração otimizada do MCP Gateway com 13 servidores principais.
#
# Copie este arquivo para .env e preencha com seus valores reais.
# =============================================================================

# =============================================================================
# CONFIGURAÇÕES BÁSICAS DO DOCKER
# =============================================================================
# Socket do Docker para comunicação com o daemon
DOCKER_HOST=unix:///var/run/docker.sock

# Nível de log para o MCP Gateway
# Opções: debug, info, warn, error
DOCKER_MCP_LOG_LEVEL=info

# Perfil de otimização para diferentes cenários
# Opções: performance, memory, balanced, vscode, cursor, continue, zed, lmstudio
DOCKER_MCP_OPTIMIZATION=balanced

# =============================================================================
# SERVIDORES DE PESQUISA E PESQUISA (3 servidores)
# =============================================================================

# DuckDuckGo Search - Não requer configuração adicional
# Usado para pesquisas web privativas

# Wikipedia Enhanced - Não requer configuração adicional  
# Usado para consultas enciclopédicas

# Brave Search - Opcional, requer chave de API
# Obtenha em: https://brave.com/search/api/
BRAVE_API_KEY=seu_brave_api_key_aqui

# =============================================================================
# SERVIDORES DE DESENVOLVIMENTO (4 servidores)
# =============================================================================

# GitHub Integration - Obrigatório para desenvolvimento
# Obtenha em: GitHub Settings > Developer settings > Personal access tokens
GITHUB_PERSONAL_ACCESS_TOKEN=ghp_seu_github_token_aqui

# Filesystem Operations - Não requer configuração adicional
# Usado para operações de arquivos locais

# Kubernetes Cluster Management - Obrigatório para DevOps
# Caminho para o arquivo kubeconfig
KUBECONFIG_PATH=/home/user/.kube/config

# Context7 Documentation - Não requer configuração adicional
# Usado para documentação de código atualizada

# =============================================================================
# SERVIDORES DE BANCOS DE DADOS (4 servidores)
# =============================================================================

# PostgreSQL Database - Obrigatório para maioria dos casos de uso
# Formato: postgresql://username:password@host:port/database
POSTGRES_CONNECTION_STRING=postgresql://postgres:senha@localhost:5432/mcpdb

# Redis Cache - Obrigatório para cache e sessões
# Formato: redis://username:password@host:port/database (senha opcional)
REDIS_CONNECTION_STRING=redis://localhost:6379

# ClickHouse Analytics - Opcional para analytics avançados
# Formato: clickhouse://username:password@host:port/database
CLICKHOUSE_CONNECTION_STRING=clickhouse://default:senha@localhost:8123/analytics

# SQLite Advanced - Não requer configuração adicional
# Usado para banco local com busca vetorial e geoespacial

# =============================================================================
# SERVIDORES DE COMUNICAÇÃO (2 servidores)
# =============================================================================

# Slack Communication - Opcional para integração com equipes
# Obtenha em: https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-seu-slack-bot-token-aqui

# Discord Platform - Opcional para comunidades gaming/tech
# Obtenha em: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=Bot-seu-discord-bot-token-aqui

# =============================================================================
# SERVIDORES DE PRODUTIVIDADE (1 servidor)
# =============================================================================

# Notion Productivity - Opcional para gestão de conhecimento
# Obtenha em: https://www.notion.so/my-integrations
NOTION_API_KEY=secret_seu-notion-integration-token-aqui

# =============================================================================
# CONFIGURAÇÕES AVANÇADAS (Opcional)
# =============================================================================

# Caminhos para arquivos de configuração personalizados
DOCKER_MCP_CONFIG_PATH=~/.docker/mcp/config.yaml
DOCKER_MCP_CATALOG_PATH=~/.docker/mcp/catalogs/docker-mcp.yaml
DOCKER_MCP_REGISTRY_PATH=~/.docker/mcp/registry.yaml

# Limites de recursos (opcional)
DOCKER_MCP_CPU_LIMIT=2
DOCKER_MCP_MEMORY_LIMIT=4Gb
DOCKER_MCP_TIMEOUT=60s

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA (Opcional)
# =============================================================================

# Bloquear acesso à rede (recomendado para produção)
DOCKER_MCP_BLOCK_NETWORK=false

# Bloquear envio de secrets (recomendado)
DOCKER_MCP_BLOCK_SECRETS=true

# Verificar assinaturas das imagens (recomendado para produção)
DOCKER_MCP_VERIFY_SIGNATURES=false

# Modo sandbox (experimental)
DOCKER_MCP_SANDBOX_MODE=false

# =============================================================================
# CONFIGURAÇÕES DE MONITORAMENTO (Opcional)
# =============================================================================

# Habilitar monitoramento
DOCKER_MCP_MONITORING_ENABLED=false

# Endpoint de métricas (se habilitado)
DOCKER_MCP_METRICS_ENDPOINT=http://localhost:9090/metrics

# Intervalo de health checks
DOCKER_MCP_HEALTH_CHECK_INTERVAL=30s

# =============================================================================
# EXEMPLOS DE CONFIGURAÇÃO POR CASO DE USO
# =============================================================================

# Para Desenvolvedor Full Stack:
# Descomente as variáveis necessárias:
# - GITHUB_PERSONAL_ACCESS_TOKEN
# - POSTGRES_CONNECTION_STRING  
# - REDIS_CONNECTION_STRING
# - KUBECONFIG_PATH (opcional)

# Para Analista de Dados:
# Descomente as variáveis necessárias:
# - POSTGRES_CONNECTION_STRING
# - CLICKHOUSE_CONNECTION_STRING
# - NOTION_API_KEY (opcional)
# - BRAVE_API_KEY (opcional)

# Para Engenheiro DevOps:
# Descomente as variáveis necessárias:
# - KUBECONFIG_PATH
# - SLACK_BOT_TOKEN
# - DISCORD_BOT_TOKEN
# - GITHUB_PERSONAL_ACCESS_TOKEN
# - POSTGRES_CONNECTION_STRING

# Para Pesquisador:
# Descomente as variáveis necessárias:
# - GITHUB_PERSONAL_ACCESS_TOKEN
# - NOTION_API_KEY
# - BRAVE_API_KEY (opcional)

# =============================================================================
# INSTRUÇÕES DE USO
# =============================================================================

# 1. Copie este arquivo:
#    cp .env-optimized.example .env

# 2. Edite o arquivo .env com seus valores reais

# 3. Carregue as variáveis de ambiente:
#    source .env
#    # ou
#    export $(cat .env | xargs)

# 4. Execute o MCP Gateway:
#    docker mcp gateway run --servers=duckduckgo,wikipedia-mcp,brave,github,filesystem,kubernetes,context7,postgres,redis,clickhouse,sqlite-mcp-server,slack,mcp-discord,notion

# 5. Para configuração específica, use os casos de uso:
#    docker mcp gateway run --use-case=full_stack_developer
#    docker mcp gateway run --use-case=data_analyst
#    docker mcp gateway run --use-case=devops_engineer
#    docker mcp gateway run --use-case=researcher

# =============================================================================
# SOLUÇÃO DE PROBLEMAS
# =============================================================================

# Se encontrar problemas de permissão com Docker:
# sudo usermod -aG docker $USER
# # Faça logout e login novamente

# Se os servidores não forem encontrados:
# docker mcp catalog show
# docker mcp gateway run --verbose --dry-run

# Se houver problemas de autenticação:
# docker mcp gateway run --secrets=docker-desktop:./.env

# =============================================================================
# INFORMAÇÕES ADICIONAIS
# =============================================================================

# Documentação completa: mcp-configs/README-optimized.md
# Configuração principal: mcp-configs/mcp-gateway-config-optimized.json
# Exemplos de uso: mcp-configs/docker-compose-mcp-gateway.yml

# Suporte:
# - Docker MCP Gateway: https://github.com/docker/mcp-gateway
# - MCP Protocol: https://modelcontextprotocol.io/
# - Comunidade Discord: https://discord.gg/docker